# Server environment variables example
# MongoDB connection string
MONGO_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/quizgame?retryWrites=true&w=majority

# JWT secret used to sign tokens
JWT_SECRET=your_jwt_secret_here

# Port the server listens on (Render will provide PORT)
PORT=5000

# Frontend URL(s) allowed by CORS. Comma-separated for multiple origins.
# Example: FRONTEND_URL=https://quizgame-frontend.onrender.com,http://localhost:5173
FRONTEND_URL=
